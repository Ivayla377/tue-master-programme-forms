// Mounts the CYBR form through the shared SurveyJS application shell.
import { mountProgramForm } from "../shared/app.js";
import { cybrFormConfig } from "../programmes/cybr/index.js";

mountProgramForm(cybrFormConfig);
