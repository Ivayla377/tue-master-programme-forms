// Mounts the ES form through the shared SurveyJS application shell.
import { mountProgramForm } from "../shared/app.js";
import { esFormConfig } from "../programmes/es/index.js";

mountProgramForm(esFormConfig);
