// Mounts the CSE form through the shared SurveyJS application shell.
import { mountProgramForm } from "../shared/app.js";
import { cseFormConfig } from "../programmes/cse/index.js";

mountProgramForm(cseFormConfig);
