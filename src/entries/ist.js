// Mounts the IST form through the shared SurveyJS application shell.
import { mountProgramForm } from "../shared/app.js";
import { istFormConfig } from "../programmes/ist/index.js";

mountProgramForm(istFormConfig);
